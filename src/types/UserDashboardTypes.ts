export interface News {
  id: string;
  title: string;
  url: string;
  imageUrl: string;
}

export interface NFT {
  id: string;
  name: string;
  imageUrl: string;
  description: string;
  owner: string;
  price: number;
}
